# Phase 0 — クローンと Amplify ベースライン

構成案: Phase 0 / 記事セクション 1 相当

## 目的

公式 [amplify-vite-react-template](https://github.com/aws-samples/amplify-vite-react-template) の Todo アプリを、改変前の状態で動かし「Before」を記録する。

## 手順

（実行後に `commands/` ログと `snapshots/` を追記）

## 学び

- Amplify Gen 2 の `defineData` + `client.models.Todo.*`
- Todo は `publicApiKey()` — Auth 定義と未接続

## 対応 git tag

`phase-0-amplify-baseline`
