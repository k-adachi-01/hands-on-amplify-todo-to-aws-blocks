# コピーして .env.local として使う（.env.local は gitignore 済み）
# cp .env.local.example .env.local

# SSO ログイン後も ampx / AWS SDK はプロファイル名が必要
AWS_PROFILE=aws-poc-sandbox

# 任意: リージョン（プロファイル側に設定済みなら不要）
# AWS_REGION=ap-northeast-1
